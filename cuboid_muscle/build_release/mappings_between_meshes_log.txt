* Parse settings for mapping between meshes "mesh3D" -> "fiber0".
* Parse settings for mapping between meshes "mesh3D" -> "fiber10".
* Parse settings for mapping between meshes "mesh3D" -> "fiber20".
* Parse settings for mapping between meshes "mesh3D" -> "fiber30".
* Parse settings for mapping between meshes "mesh3D" -> "fiber40".
* Parse settings for mapping between meshes "mesh3D" -> "fiber50".
* Parse settings for mapping between meshes "mesh3D" -> "fiber60".
* Parse settings for mapping between meshes "mesh3D" -> "fiber70".
* Parse settings for mapping between meshes "mesh3D" -> "fiber80".
* Parse settings for mapping between meshes "mesh3D" -> "fiber90".
* Parse settings for mapping between meshes "mesh3D" -> "fiber1".
* Parse settings for mapping between meshes "mesh3D" -> "fiber11".
* Parse settings for mapping between meshes "mesh3D" -> "fiber21".
* Parse settings for mapping between meshes "mesh3D" -> "fiber31".
* Parse settings for mapping between meshes "mesh3D" -> "fiber41".
* Parse settings for mapping between meshes "mesh3D" -> "fiber51".
* Parse settings for mapping between meshes "mesh3D" -> "fiber61".
* Parse settings for mapping between meshes "mesh3D" -> "fiber71".
* Parse settings for mapping between meshes "mesh3D" -> "fiber81".
* Parse settings for mapping between meshes "mesh3D" -> "fiber91".
* Parse settings for mapping between meshes "mesh3D" -> "fiber2".
* Parse settings for mapping between meshes "mesh3D" -> "fiber12".
* Parse settings for mapping between meshes "mesh3D" -> "fiber22".
* Parse settings for mapping between meshes "mesh3D" -> "fiber32".
* Parse settings for mapping between meshes "mesh3D" -> "fiber42".
* Parse settings for mapping between meshes "mesh3D" -> "fiber52".
* Parse settings for mapping between meshes "mesh3D" -> "fiber62".
* Parse settings for mapping between meshes "mesh3D" -> "fiber72".
* Parse settings for mapping between meshes "mesh3D" -> "fiber82".
* Parse settings for mapping between meshes "mesh3D" -> "fiber92".
* Parse settings for mapping between meshes "mesh3D" -> "fiber3".
* Parse settings for mapping between meshes "mesh3D" -> "fiber13".
* Parse settings for mapping between meshes "mesh3D" -> "fiber23".
* Parse settings for mapping between meshes "mesh3D" -> "fiber33".
* Parse settings for mapping between meshes "mesh3D" -> "fiber43".
* Parse settings for mapping between meshes "mesh3D" -> "fiber53".
* Parse settings for mapping between meshes "mesh3D" -> "fiber63".
* Parse settings for mapping between meshes "mesh3D" -> "fiber73".
* Parse settings for mapping between meshes "mesh3D" -> "fiber83".
* Parse settings for mapping between meshes "mesh3D" -> "fiber93".
* Parse settings for mapping between meshes "mesh3D" -> "fiber4".
* Parse settings for mapping between meshes "mesh3D" -> "fiber14".
* Parse settings for mapping between meshes "mesh3D" -> "fiber24".
* Parse settings for mapping between meshes "mesh3D" -> "fiber34".
* Parse settings for mapping between meshes "mesh3D" -> "fiber44".
* Parse settings for mapping between meshes "mesh3D" -> "fiber54".
* Parse settings for mapping between meshes "mesh3D" -> "fiber64".
* Parse settings for mapping between meshes "mesh3D" -> "fiber74".
* Parse settings for mapping between meshes "mesh3D" -> "fiber84".
* Parse settings for mapping between meshes "mesh3D" -> "fiber94".
* Parse settings for mapping between meshes "mesh3D" -> "fiber5".
* Parse settings for mapping between meshes "mesh3D" -> "fiber15".
* Parse settings for mapping between meshes "mesh3D" -> "fiber25".
* Parse settings for mapping between meshes "mesh3D" -> "fiber35".
* Parse settings for mapping between meshes "mesh3D" -> "fiber45".
* Parse settings for mapping between meshes "mesh3D" -> "fiber55".
* Parse settings for mapping between meshes "mesh3D" -> "fiber65".
* Parse settings for mapping between meshes "mesh3D" -> "fiber75".
* Parse settings for mapping between meshes "mesh3D" -> "fiber85".
* Parse settings for mapping between meshes "mesh3D" -> "fiber95".
* Parse settings for mapping between meshes "mesh3D" -> "fiber6".
* Parse settings for mapping between meshes "mesh3D" -> "fiber16".
* Parse settings for mapping between meshes "mesh3D" -> "fiber26".
* Parse settings for mapping between meshes "mesh3D" -> "fiber36".
* Parse settings for mapping between meshes "mesh3D" -> "fiber46".
* Parse settings for mapping between meshes "mesh3D" -> "fiber56".
* Parse settings for mapping between meshes "mesh3D" -> "fiber66".
* Parse settings for mapping between meshes "mesh3D" -> "fiber76".
* Parse settings for mapping between meshes "mesh3D" -> "fiber86".
* Parse settings for mapping between meshes "mesh3D" -> "fiber96".
* Parse settings for mapping between meshes "mesh3D" -> "fiber7".
* Parse settings for mapping between meshes "mesh3D" -> "fiber17".
* Parse settings for mapping between meshes "mesh3D" -> "fiber27".
* Parse settings for mapping between meshes "mesh3D" -> "fiber37".
* Parse settings for mapping between meshes "mesh3D" -> "fiber47".
* Parse settings for mapping between meshes "mesh3D" -> "fiber57".
* Parse settings for mapping between meshes "mesh3D" -> "fiber67".
* Parse settings for mapping between meshes "mesh3D" -> "fiber77".
* Parse settings for mapping between meshes "mesh3D" -> "fiber87".
* Parse settings for mapping between meshes "mesh3D" -> "fiber97".
* Parse settings for mapping between meshes "mesh3D" -> "fiber8".
* Parse settings for mapping between meshes "mesh3D" -> "fiber18".
* Parse settings for mapping between meshes "mesh3D" -> "fiber28".
* Parse settings for mapping between meshes "mesh3D" -> "fiber38".
* Parse settings for mapping between meshes "mesh3D" -> "fiber48".
* Parse settings for mapping between meshes "mesh3D" -> "fiber58".
* Parse settings for mapping between meshes "mesh3D" -> "fiber68".
* Parse settings for mapping between meshes "mesh3D" -> "fiber78".
* Parse settings for mapping between meshes "mesh3D" -> "fiber88".
* Parse settings for mapping between meshes "mesh3D" -> "fiber98".
* Parse settings for mapping between meshes "mesh3D" -> "fiber9".
* Parse settings for mapping between meshes "mesh3D" -> "fiber19".
* Parse settings for mapping between meshes "mesh3D" -> "fiber29".
* Parse settings for mapping between meshes "mesh3D" -> "fiber39".
* Parse settings for mapping between meshes "mesh3D" -> "fiber49".
* Parse settings for mapping between meshes "mesh3D" -> "fiber59".
* Parse settings for mapping between meshes "mesh3D" -> "fiber69".
* Parse settings for mapping between meshes "mesh3D" -> "fiber79".
* Parse settings for mapping between meshes "mesh3D" -> "fiber89".
* Parse settings for mapping between meshes "mesh3D" -> "fiber99".
* Create mapping between meshes "fiber0" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1100/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.00397392 s.
* Create mapping between meshes "fiber10" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.00764251 s.
* Create mapping between meshes "fiber20" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0112867 s.
* Create mapping between meshes "fiber30" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0167883 s.
* Create mapping between meshes "fiber40" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0221787 s.
* Create mapping between meshes "fiber50" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0270046 s.
* Create mapping between meshes "fiber60" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              990/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0275408 s.
* Create mapping between meshes "fiber70" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0280492 s.
* Create mapping between meshes "fiber80" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0288668 s.
* Create mapping between meshes "fiber90" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0330409 s.
* Create mapping between meshes "fiber1" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0334844 s.
* Create mapping between meshes "fiber11" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0340236 s.
* Create mapping between meshes "fiber21" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0345465 s.
* Create mapping between meshes "fiber31" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0350081 s.
* Create mapping between meshes "fiber41" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0354508 s.
* Create mapping between meshes "fiber51" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.03591 s.
* Create mapping between meshes "fiber61" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              982/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0363889 s.
* Create mapping between meshes "fiber71" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0372033 s.
* Create mapping between meshes "fiber81" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0380618 s.
* Create mapping between meshes "fiber91" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0421745 s.
* Create mapping between meshes "fiber2" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0426395 s.
* Create mapping between meshes "fiber12" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0430931 s.
* Create mapping between meshes "fiber22" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0435484 s.
* Create mapping between meshes "fiber32" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0440033 s.
* Create mapping between meshes "fiber42" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0444655 s.
* Create mapping between meshes "fiber52" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0449242 s.
* Create mapping between meshes "fiber62" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              983/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0454426 s.
* Create mapping between meshes "fiber72" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0462535 s.
* Create mapping between meshes "fiber82" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0470624 s.
* Create mapping between meshes "fiber92" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0511534 s.
* Create mapping between meshes "fiber3" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0538788 s.
* Create mapping between meshes "fiber13" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0543409 s.
* Create mapping between meshes "fiber23" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0548177 s.
* Create mapping between meshes "fiber33" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.055279 s.
* Create mapping between meshes "fiber43" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0557381 s.
* Create mapping between meshes "fiber53" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0562143 s.
* Create mapping between meshes "fiber63" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              980/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0566934 s.
* Create mapping between meshes "fiber73" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0575526 s.
* Create mapping between meshes "fiber83" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0583702 s.
* Create mapping between meshes "fiber93" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              880/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0633291 s.
* Create mapping between meshes "fiber4" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0689536 s.
* Create mapping between meshes "fiber14" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0694139 s.
* Create mapping between meshes "fiber24" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0699128 s.
* Create mapping between meshes "fiber34" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0703736 s.
* Create mapping between meshes "fiber44" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.070834 s.
* Create mapping between meshes "fiber54" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0712929 s.
* Create mapping between meshes "fiber64" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              986/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0717761 s.
* Create mapping between meshes "fiber74" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0726639 s.
* Create mapping between meshes "fiber84" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0734876 s.
* Create mapping between meshes "fiber94" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0794098 s.
* Create mapping between meshes "fiber5" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0850096 s.
* Create mapping between meshes "fiber15" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.085505 s.
* Create mapping between meshes "fiber25" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0859702 s.
* Create mapping between meshes "fiber35" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0864276 s.
* Create mapping between meshes "fiber45" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.086887 s.
* Create mapping between meshes "fiber55" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0873459 s.
* Create mapping between meshes "fiber65" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              984/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0878749 s.
* Create mapping between meshes "fiber75" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0883902 s.
* Create mapping between meshes "fiber85" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0888933 s.
* Create mapping between meshes "fiber95" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0948649 s.
* Create mapping between meshes "fiber6" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              990/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0986162 s.
* Create mapping between meshes "fiber16" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              982/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0991199 s.
* Create mapping between meshes "fiber26" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              982/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.0995999 s.
* Create mapping between meshes "fiber36" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              983/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.10012 s.
* Create mapping between meshes "fiber46" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              982/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.100617 s.
* Create mapping between meshes "fiber56" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              982/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.1011 s.
* Create mapping between meshes "fiber66" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              977/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.101596 s.
* Create mapping between meshes "fiber76" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.102114 s.
* Create mapping between meshes "fiber86" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.102628 s.
* Create mapping between meshes "fiber96" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              874/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.107616 s.
* Create mapping between meshes "fiber7" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.111741 s.
* Create mapping between meshes "fiber17" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.112245 s.
* Create mapping between meshes "fiber27" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.112784 s.
* Create mapping between meshes "fiber37" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.11329 s.
* Create mapping between meshes "fiber47" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.113796 s.
* Create mapping between meshes "fiber57" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.114311 s.
* Create mapping between meshes "fiber67" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.114831 s.
* Create mapping between meshes "fiber77" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.115345 s.
* Create mapping between meshes "fiber87" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.115877 s.
* Create mapping between meshes "fiber97" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.120007 s.
* Create mapping between meshes "fiber8" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.12412 s.
* Create mapping between meshes "fiber18" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.124614 s.
* Create mapping between meshes "fiber28" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.125107 s.
* Create mapping between meshes "fiber38" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.125683 s.
* Create mapping between meshes "fiber48" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.126263 s.
* Create mapping between meshes "fiber58" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.126839 s.
* Create mapping between meshes "fiber68" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.127398 s.
* Create mapping between meshes "fiber78" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.127912 s.
* Create mapping between meshes "fiber88" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.128446 s.
* Create mapping between meshes "fiber98" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.132529 s.
* Create mapping between meshes "fiber9" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.136527 s.
* Create mapping between meshes "fiber19" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.1405 s.
* Create mapping between meshes "fiber29" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.144487 s.
* Create mapping between meshes "fiber39" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              886/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.149334 s.
* Create mapping between meshes "fiber49" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.155089 s.
* Create mapping between meshes "fiber59" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.160827 s.
* Create mapping between meshes "fiber69" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              862/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.165649 s.
* Create mapping between meshes "fiber79" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.169633 s.
* Create mapping between meshes "fiber89" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.173626 s.
* Create mapping between meshes "fiber99" (1D) -> "3Dmesh_quadratic" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.177621 s.
* Map from field variable "u" component 0, mesh "3Dmesh_quadratic" (3D)  [and other field variables/meshes, in total 100]
        to field variable "solution" component 0, mesh "fiber0" (1D)
  using reverse direction of mapping "fiber0" (1D) -> "3Dmesh_quadratic" (3D).
  Description: Simply interpolate in the mesh "3Dmesh_quadratic".
* Create mapping between meshes "fiber0" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.178079 s.
* Create mapping between meshes "fiber10" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.178508 s.
* Create mapping between meshes "fiber20" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.178937 s.
* Create mapping between meshes "fiber30" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.179361 s.
* Create mapping between meshes "fiber40" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.17983 s.
* Create mapping between meshes "fiber50" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.180262 s.
* Create mapping between meshes "fiber60" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.180688 s.
* Create mapping between meshes "fiber70" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.181168 s.
* Create mapping between meshes "fiber80" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.181697 s.
* Create mapping between meshes "fiber90" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.182169 s.
* Create mapping between meshes "fiber1" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.1826 s.
* Create mapping between meshes "fiber11" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.183032 s.
* Create mapping between meshes "fiber21" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.183489 s.
* Create mapping between meshes "fiber31" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.183919 s.
* Create mapping between meshes "fiber41" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.184353 s.
* Create mapping between meshes "fiber51" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.184786 s.
* Create mapping between meshes "fiber61" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.185219 s.
* Create mapping between meshes "fiber71" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.185697 s.
* Create mapping between meshes "fiber81" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.186175 s.
* Create mapping between meshes "fiber91" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.186701 s.
* Create mapping between meshes "fiber2" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.187129 s.
* Create mapping between meshes "fiber12" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.187588 s.
* Create mapping between meshes "fiber22" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.18802 s.
* Create mapping between meshes "fiber32" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.188447 s.
* Create mapping between meshes "fiber42" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.18888 s.
* Create mapping between meshes "fiber52" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.189313 s.
* Create mapping between meshes "fiber62" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.189743 s.
* Create mapping between meshes "fiber72" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.190248 s.
* Create mapping between meshes "fiber82" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.190726 s.
* Create mapping between meshes "fiber92" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.191203 s.
* Create mapping between meshes "fiber3" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.191642 s.
* Create mapping between meshes "fiber13" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.192105 s.
* Create mapping between meshes "fiber23" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.192549 s.
* Create mapping between meshes "fiber33" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.192985 s.
* Create mapping between meshes "fiber43" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.193434 s.
* Create mapping between meshes "fiber53" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.193954 s.
* Create mapping between meshes "fiber63" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.194393 s.
* Create mapping between meshes "fiber73" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.194887 s.
* Create mapping between meshes "fiber83" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.195378 s.
* Create mapping between meshes "fiber93" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.195859 s.
* Create mapping between meshes "fiber4" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.196302 s.
* Create mapping between meshes "fiber14" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.196745 s.
* Create mapping between meshes "fiber24" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.19719 s.
* Create mapping between meshes "fiber34" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.197633 s.
* Create mapping between meshes "fiber44" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.198077 s.
* Create mapping between meshes "fiber54" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.198557 s.
* Create mapping between meshes "fiber64" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.198998 s.
* Create mapping between meshes "fiber74" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.199491 s.
* Create mapping between meshes "fiber84" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.199984 s.
* Create mapping between meshes "fiber94" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.200475 s.
* Create mapping between meshes "fiber5" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.200918 s.
* Create mapping between meshes "fiber15" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.201362 s.
* Create mapping between meshes "fiber25" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.201805 s.
* Create mapping between meshes "fiber35" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.202279 s.
* Create mapping between meshes "fiber45" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.202723 s.
* Create mapping between meshes "fiber55" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.203165 s.
* Create mapping between meshes "fiber65" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.203608 s.
* Create mapping between meshes "fiber75" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.204102 s.
* Create mapping between meshes "fiber85" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.204594 s.
* Create mapping between meshes "fiber95" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.205087 s.
* Create mapping between meshes "fiber6" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.205567 s.
* Create mapping between meshes "fiber16" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.20601 s.
* Create mapping between meshes "fiber26" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.206457 s.
* Create mapping between meshes "fiber36" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.206883 s.
* Create mapping between meshes "fiber46" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.207314 s.
* Create mapping between meshes "fiber56" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.207747 s.
* Create mapping between meshes "fiber66" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 0 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.208232 s.
* Create mapping between meshes "fiber76" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.208712 s.
* Create mapping between meshes "fiber86" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.209192 s.
* Create mapping between meshes "fiber96" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.209663 s.
* Create mapping between meshes "fiber7" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.210129 s.
* Create mapping between meshes "fiber17" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.210594 s.
* Create mapping between meshes "fiber27" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.211117 s.
* Create mapping between meshes "fiber37" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.211582 s.
* Create mapping between meshes "fiber47" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.212056 s.
* Create mapping between meshes "fiber57" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.21253 s.
* Create mapping between meshes "fiber67" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.213003 s.
* Create mapping between meshes "fiber77" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.213507 s.
* Create mapping between meshes "fiber87" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.214012 s.
* Create mapping between meshes "fiber97" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.214524 s.
* Create mapping between meshes "fiber8" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.215007 s.
* Create mapping between meshes "fiber18" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.215508 s.
* Create mapping between meshes "fiber28" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.215989 s.
* Create mapping between meshes "fiber38" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.216468 s.
* Create mapping between meshes "fiber48" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.216962 s.
* Create mapping between meshes "fiber58" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.21745 s.
* Create mapping between meshes "fiber68" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.217973 s.
* Create mapping between meshes "fiber78" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.218476 s.
* Create mapping between meshes "fiber88" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1000/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.218984 s.
* Create mapping between meshes "fiber98" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.219515 s.
* Create mapping between meshes "fiber9" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.219995 s.
* Create mapping between meshes "fiber19" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.220482 s.
* Create mapping between meshes "fiber29" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.220958 s.
* Create mapping between meshes "fiber39" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.221517 s.
* Create mapping between meshes "fiber49" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.222005 s.
* Create mapping between meshes "fiber59" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.2225 s.
* Create mapping between meshes "fiber69" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.222995 s.
* Create mapping between meshes "fiber79" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.223504 s.
* Create mapping between meshes "fiber89" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1150/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.224006 s.
* Create mapping between meshes "fiber99" (1D) -> "mesh3D" (3D).
  Statistics: 0/100 source dofs are outside the target mesh,
              1200/1225 target dofs are outside the source mesh (fixing this is disabled),
              iterated 1 times over target mesh,
              "xiTolerance": 0.1 (increase this value to reduce the number of (costly) iterations over the whole mesh, however increasing potentially leads to more elements being checked which takes longer).
              Total duration of all mappings so far: 0.224499 s.
* Map from field variable "Razumova/activestress" component 0, mesh "fiber0" (1D)  [and other field variables/meshes, in total 100]
        to field variable "γ" component 0, mesh "mesh3D" (3D)
  using forward mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Collection and normalization of the contributions from points in mesh "fiber0" that are located in adjacent elements to the target dof in the target mesh "mesh3D".
* Map from field variable "geometry", all components, mesh "mesh3D" (3D)  [and other field variables/meshes, in total 100]
        to field variable "geometry", all components, mesh "fiber0" (1D)
  using reverse direction of mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Simply interpolate in the mesh "mesh3D".
* Map from field variable "Razumova/activestress" component 0, mesh "fiber0" (1D)  [and other field variables/meshes, in total 100]
        to field variable "γ" component 0, mesh "mesh3D" (3D)
  using forward mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Collection and normalization of the contributions from points in mesh "fiber0" that are located in adjacent elements to the target dof in the target mesh "mesh3D".
* Map from field variable "geometry", all components, mesh "mesh3D" (3D)  [and other field variables/meshes, in total 100]
        to field variable "geometry", all components, mesh "fiber0" (1D)
  using reverse direction of mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Simply interpolate in the mesh "mesh3D".
* Map from field variable "Razumova/activestress" component 0, mesh "fiber0" (1D)  [and other field variables/meshes, in total 100]
        to field variable "γ" component 0, mesh "mesh3D" (3D)
  using forward mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Collection and normalization of the contributions from points in mesh "fiber0" that are located in adjacent elements to the target dof in the target mesh "mesh3D".
* Map from field variable "geometry", all components, mesh "mesh3D" (3D)  [and other field variables/meshes, in total 100]
        to field variable "geometry", all components, mesh "fiber0" (1D)
  using reverse direction of mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Simply interpolate in the mesh "mesh3D".
* Map from field variable "Razumova/activestress" component 0, mesh "fiber0" (1D)  [and other field variables/meshes, in total 100]
        to field variable "γ" component 0, mesh "mesh3D" (3D)
  using forward mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Collection and normalization of the contributions from points in mesh "fiber0" that are located in adjacent elements to the target dof in the target mesh "mesh3D".
* Map from field variable "geometry", all components, mesh "mesh3D" (3D)  [and other field variables/meshes, in total 100]
        to field variable "geometry", all components, mesh "fiber0" (1D)
  using reverse direction of mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Simply interpolate in the mesh "mesh3D".
* Map from field variable "Razumova/activestress" component 0, mesh "fiber0" (1D)  [and other field variables/meshes, in total 100]
        to field variable "γ" component 0, mesh "mesh3D" (3D)
  using forward mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Collection and normalization of the contributions from points in mesh "fiber0" that are located in adjacent elements to the target dof in the target mesh "mesh3D".
* Map from field variable "geometry", all components, mesh "mesh3D" (3D)  [and other field variables/meshes, in total 100]
        to field variable "geometry", all components, mesh "fiber0" (1D)
  using reverse direction of mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Simply interpolate in the mesh "mesh3D".
* Map from field variable "Razumova/activestress" component 0, mesh "fiber0" (1D)  [and other field variables/meshes, in total 100]
        to field variable "γ" component 0, mesh "mesh3D" (3D)
  using forward mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Collection and normalization of the contributions from points in mesh "fiber0" that are located in adjacent elements to the target dof in the target mesh "mesh3D".
* Map from field variable "geometry", all components, mesh "mesh3D" (3D)  [and other field variables/meshes, in total 100]
        to field variable "geometry", all components, mesh "fiber0" (1D)
  using reverse direction of mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Simply interpolate in the mesh "mesh3D".
* Map from field variable "Razumova/activestress" component 0, mesh "fiber0" (1D)  [and other field variables/meshes, in total 100]
        to field variable "γ" component 0, mesh "mesh3D" (3D)
  using forward mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Collection and normalization of the contributions from points in mesh "fiber0" that are located in adjacent elements to the target dof in the target mesh "mesh3D".
* Map from field variable "geometry", all components, mesh "mesh3D" (3D)  [and other field variables/meshes, in total 100]
        to field variable "geometry", all components, mesh "fiber0" (1D)
  using reverse direction of mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Simply interpolate in the mesh "mesh3D".
* Map from field variable "Razumova/activestress" component 0, mesh "fiber0" (1D)  [and other field variables/meshes, in total 100]
        to field variable "γ" component 0, mesh "mesh3D" (3D)
  using forward mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Collection and normalization of the contributions from points in mesh "fiber0" that are located in adjacent elements to the target dof in the target mesh "mesh3D".
* Map from field variable "geometry", all components, mesh "mesh3D" (3D)  [and other field variables/meshes, in total 100]
        to field variable "geometry", all components, mesh "fiber0" (1D)
  using reverse direction of mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Simply interpolate in the mesh "mesh3D".
* Map from field variable "Razumova/activestress" component 0, mesh "fiber0" (1D)  [and other field variables/meshes, in total 100]
        to field variable "γ" component 0, mesh "mesh3D" (3D)
  using forward mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Collection and normalization of the contributions from points in mesh "fiber0" that are located in adjacent elements to the target dof in the target mesh "mesh3D".
* Map from field variable "geometry", all components, mesh "mesh3D" (3D)  [and other field variables/meshes, in total 100]
        to field variable "geometry", all components, mesh "fiber0" (1D)
  using reverse direction of mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Simply interpolate in the mesh "mesh3D".
* Map from field variable "Razumova/activestress" component 0, mesh "fiber0" (1D)  [and other field variables/meshes, in total 100]
        to field variable "γ" component 0, mesh "mesh3D" (3D)
  using forward mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Collection and normalization of the contributions from points in mesh "fiber0" that are located in adjacent elements to the target dof in the target mesh "mesh3D".
* Map from field variable "geometry", all components, mesh "mesh3D" (3D)  [and other field variables/meshes, in total 100]
        to field variable "geometry", all components, mesh "fiber0" (1D)
  using reverse direction of mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Simply interpolate in the mesh "mesh3D".
* Map from field variable "Razumova/activestress" component 0, mesh "fiber0" (1D)  [and other field variables/meshes, in total 100]
        to field variable "γ" component 0, mesh "mesh3D" (3D)
  using forward mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Collection and normalization of the contributions from points in mesh "fiber0" that are located in adjacent elements to the target dof in the target mesh "mesh3D".
* Map from field variable "geometry", all components, mesh "mesh3D" (3D)  [and other field variables/meshes, in total 100]
        to field variable "geometry", all components, mesh "fiber0" (1D)
  using reverse direction of mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Simply interpolate in the mesh "mesh3D".
* Map from field variable "Razumova/activestress" component 0, mesh "fiber0" (1D)  [and other field variables/meshes, in total 100]
        to field variable "γ" component 0, mesh "mesh3D" (3D)
  using forward mapping "fiber0" (1D) -> "mesh3D" (3D).
  Description: Collection and normalization of the contributions from points in mesh "fiber0" that are located in adjacent elements to the target dof in the target mesh "mesh3D".
